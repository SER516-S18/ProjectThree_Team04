package edu.asu.ser516.projecttwo.team04.ui;

/**
 * AppClient, the main UI for the client application, extending the AppBase UI (with its console and other similarities)
 */
public class AppClient extends AppBase {
    public AppClient(App parent) {
        super(parent);
    }
}