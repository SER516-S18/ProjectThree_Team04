package edu.asu.ser516.projecttwo.team04.ui;

/**
 * AppServer, the main UI for the server application, extending the AppBase UI (with its console and other similarities)
 */
public class AppServer extends AppBase {
    public AppServer(App parent) {
        super(parent);
    }
}
